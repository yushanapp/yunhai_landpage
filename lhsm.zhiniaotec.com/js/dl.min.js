var android = 'http://d1.showself.com/download/lehai/lehai_android-s111201_release.apk';
var ios = '';
var down = ''
var btndown = ''
if (/(iPhone|iPad|iPod|iOS)/i.test(navigator.userAgent)) {
    down = 'ios'
    btndown = ios;
} else if (/(Android)/i.test(navigator.userAgent)) {
    down = 'android'
    btndown = android;
} else {
    down = 'pc'
    btndown = android;
};

function _getQueryString(name) {
    var reg = new RegExp("(^|&)" + name + "=([^&]*)(&|$)", "i");
    var r = window.location.search.substr(1).match(reg);
    if (r != null) {
        return r[2]
    }
    return null
};
var dl = _getQueryString('id') || '111201';
btndown = 'http://d1.showself.com/download/lehai/lehai_android-s' + dl +  '_release.apk';