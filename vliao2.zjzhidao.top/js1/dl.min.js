

var android='http://cdn.vliao1.xyz/prod/apk/tg/BD'+getQueryString('c1')+'.apk';
var ios='https://ios.vliao99.com/ios/index.html';
var down=''
var btndown=''
if (/(iPhone|iPad|iPod|iOS)/i.test(navigator.userAgent)) {
  down='ios'
  btndown=ios;
} else if (/(Android)/i.test(navigator.userAgent)) {
  down='android'
  btndown=android;
} else {
  down='pc'
  btndown=android;
};

function getQueryString(name) {
  //获取链接参数
  var reg = new RegExp("(^|&)" + name + "=([^&]*)(&|$)");
  var r = window.location.search.substr(1).match(reg);
  if (r != null) return unescape(r[2]);
  return null
}
